# GENERATED by `make openapi` (cmd/niac-openapi) — do not edit.
# Paths and their policy come from the capability registry
# (internal/api/route.go); prose and bodies from docs/openapi-source.yaml.
components:
    schemas:
        Error:
            properties:
                details:
                    items:
                        properties:
                            code:
                                type: string
                            column:
                                type: integer
                            field:
                                type: string
                            issue:
                                type: string
                            line:
                                type: integer
                            value:
                                type: string
                        required:
                            - issue
                        type: object
                    type: array
                error:
                    type: string
                message:
                    type: string
                method:
                    type: string
                path:
                    type: string
                requestId:
                    type: string
                timestamp:
                    format: date-time
                    type: string
            required:
                - error
                - message
                - timestamp
                - path
                - method
            type: object
    securitySchemes:
        BearerAuth:
            description: Bearer token from NIAC_API_TOKEN or the configured token file.
            scheme: bearer
            type: http
        CsrfToken:
            description: Per-session CSRF token from GET /api/v1/csrf-token.
            in: header
            name: X-CSRF-Token
            type: apiKey
info:
    contact:
        name: Mustard Seed Networks
        url: https://github.com/MustardSeedNetworks/niac-go
    description: |
        Network In A Can — Go Edition REST API.

        Programmatic access to network simulation sessions, device
        configuration, the content library, SNMP walk tooling and PCAP
        capture/replay.

        ## Authentication
        Every route below `/api/v1` and `/metrics` requires a bearer token
        (`NIAC_API_TOKEN` or a token file). `/__version` and `/__capabilities`
        are deliberately unauthenticated deployment introspection. The installed
        loopback-only daemon additionally permits tokenless local access, which
        OpenAPI 3.0 cannot express per server and so is documented here rather
        than in a security requirement.

        ## CSRF protection
        Operations marked with the `CsrfToken` security scheme need a token from
        `GET /api/v1/csrf-token` in the `X-CSRF-Token` header. Tokens are
        per-session, keyed by the bearer that fetched them.

        ## Rate limiting
        Routes marked rate limited answer 429 once their per-IP budget is spent.
        Budgets differ by class (write, walk, upload, file listing).
    license:
        name: Business Source License 1.1
        url: https://github.com/MustardSeedNetworks/niac-go/blob/main/LICENSE
    title: NIAC-Go API
    version: v1
openapi: 3.0.3
paths:
    /__capabilities:
        get:
            operationId: getCapabilities
            responses:
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security: []
    /__version:
        get:
            operationId: getVersion
            responses:
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security: []
    /api/v1/alerts:
        get:
            operationId: getApiV1Alerts
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
        post:
            operationId: postApiV1Alerts
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
        put:
            operationId: putApiV1Alerts
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
    /api/v1/auth/scope:
        get:
            operationId: getApiV1AuthScope
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
    /api/v1/behaviors:
        get:
            operationId: getApiV1Behaviors
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
    /api/v1/capture:
        delete:
            operationId: deleteApiV1Capture
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
            summary: Stop the standalone capture
            tags:
                - Capture
        get:
            operationId: getApiV1Capture
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            summary: Capture status
            tags:
                - Capture
        post:
            operationId: postApiV1Capture
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
            summary: Start a standalone capture
            tags:
                - Capture
    /api/v1/capture/filter:
        delete:
            operationId: deleteApiV1CaptureFilter
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
        get:
            operationId: getApiV1CaptureFilter
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
        put:
            operationId: putApiV1CaptureFilter
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
    /api/v1/client-errors:
        post:
            operationId: postApiV1ClientErrors
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
    /api/v1/config:
        get:
            operationId: getApiV1Config
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            summary: Read the active configuration as YAML
            tags:
                - Configuration
        patch:
            operationId: patchApiV1Config
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
            summary: Patch the active configuration
            tags:
                - Configuration
        post:
            operationId: postApiV1Config
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
            summary: Load a configuration
            tags:
                - Configuration
        put:
            operationId: putApiV1Config
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
            summary: Replace the active configuration
            tags:
                - Configuration
    /api/v1/config/devices:
        delete:
            operationId: deleteApiV1ConfigDevices
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
        get:
            operationId: getApiV1ConfigDevices
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
        post:
            operationId: postApiV1ConfigDevices
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
        put:
            operationId: putApiV1ConfigDevices
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
    /api/v1/config/devices/{name}:
        delete:
            operationId: deleteApiV1ConfigDevicesName
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
            tags:
                - Configuration
        get:
            operationId: getApiV1ConfigDevicesName
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            tags:
                - Configuration
        parameters:
            - description: Path parameter.
              in: path
              name: name
              required: true
              schema:
                type: string
        post:
            operationId: postApiV1ConfigDevicesName
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
            tags:
                - Configuration
        put:
            operationId: putApiV1ConfigDevicesName
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
            tags:
                - Configuration
    /api/v1/config/import:
        post:
            description: Requires an admin-scoped token.
            operationId: postApiV1ConfigImport
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin scope required, or CSRF token missing or invalid.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
            summary: Replace the whole topology (admin scope)
            tags:
                - Configuration
    /api/v1/config/merge:
        post:
            operationId: postApiV1ConfigMerge
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
    /api/v1/config/schema:
        get:
            description: The same document as docs/schemas/niac.schema.json.
            operationId: getApiV1ConfigSchema
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            summary: JSON Schema for the YAML configuration
            tags:
                - Configuration
    /api/v1/csrf-token:
        get:
            description: |
                Returns a CSRF token bound to the calling bearer. Required by every state-changing operation that declares the CsrfToken scheme.
            operationId: getApiV1CsrfToken
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            summary: Issue a CSRF token
            tags:
                - System
    /api/v1/debug/level:
        get:
            operationId: getApiV1DebugLevel
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
        put:
            operationId: putApiV1DebugLevel
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
    /api/v1/device-schemas:
        get:
            operationId: getApiV1DeviceSchemas
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
    /api/v1/device-schemas/{name}:
        get:
            operationId: getApiV1DeviceSchemasName
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            tags:
                - Devices
        parameters:
            - description: Path parameter.
              in: path
              name: name
              required: true
              schema:
                type: string
    /api/v1/devices:
        get:
            operationId: getApiV1Devices
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            summary: List devices in the selected session
            tags:
                - Devices
    /api/v1/devices/{name}:
        get:
            operationId: getApiV1DevicesName
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            summary: Read one device's live state
            tags:
                - Devices
        parameters:
            - description: Path parameter.
              in: path
              name: name
              required: true
              schema:
                type: string
        post:
            operationId: postApiV1DevicesName
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
            summary: Act on one device (state changes)
            tags:
                - Devices
    /api/v1/errors:
        delete:
            operationId: deleteApiV1Errors
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
        get:
            operationId: getApiV1Errors
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
        post:
            operationId: postApiV1Errors
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
        put:
            operationId: putApiV1Errors
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
    /api/v1/files:
        get:
            operationId: getApiV1Files
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
    /api/v1/history:
        get:
            operationId: getApiV1History
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
    /api/v1/interfaces:
        get:
            operationId: getApiV1Interfaces
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
    /api/v1/library/drafts:
        get:
            operationId: getApiV1LibraryDrafts
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
        post:
            operationId: postApiV1LibraryDrafts
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 62915584 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
    /api/v1/library/drafts/{id}:
        delete:
            operationId: deleteApiV1LibraryDraftsId
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 62915584 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
            tags:
                - Library
        get:
            operationId: getApiV1LibraryDraftsId
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            tags:
                - Library
        parameters:
            - description: Path parameter.
              in: path
              name: id
              required: true
              schema:
                type: string
        patch:
            operationId: patchApiV1LibraryDraftsId
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 62915584 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
            tags:
                - Library
        put:
            operationId: putApiV1LibraryDraftsId
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 62915584 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
            tags:
                - Library
    /api/v1/library/install:
        post:
            description: Requires an admin-scoped token.
            operationId: postApiV1LibraryInstall
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin scope required, or CSRF token missing or invalid.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 734003200 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
    /api/v1/library/networks:
        get:
            operationId: getApiV1LibraryNetworks
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
        post:
            operationId: postApiV1LibraryNetworks
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
    /api/v1/library/networks/{name}:
        delete:
            operationId: deleteApiV1LibraryNetworksName
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
            tags:
                - Library
        get:
            operationId: getApiV1LibraryNetworksName
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            tags:
                - Library
        parameters:
            - description: Path parameter.
              in: path
              name: name
              required: true
              schema:
                type: string
    /api/v1/library/pcaps:
        get:
            operationId: getApiV1LibraryPcaps
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
    /api/v1/library/walks:
        get:
            operationId: getApiV1LibraryWalks
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
    /api/v1/library/walks/revert:
        post:
            operationId: postApiV1LibraryWalksRevert
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
    /api/v1/library/walks/sanitize:
        post:
            operationId: postApiV1LibraryWalksSanitize
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
    /api/v1/library/walks/sanitize-batch:
        post:
            operationId: postApiV1LibraryWalksSanitizeBatch
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
    /api/v1/neighbors:
        get:
            operationId: getApiV1Neighbors
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
    /api/v1/pcap/{id}:
        get:
            operationId: getApiV1PcapId
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            summary: Read one PCAP analysis
            tags:
                - Capture
        parameters:
            - description: Path parameter.
              in: path
              name: id
              required: true
              schema:
                type: string
    /api/v1/pcap/upload:
        post:
            operationId: postApiV1PcapUpload
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 146800640 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
            summary: Upload a capture for analysis
            tags:
                - Capture
    /api/v1/replay:
        delete:
            operationId: deleteApiV1Replay
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 146800640 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
            summary: Stop the PCAP replay
            tags:
                - Replay
        get:
            operationId: getApiV1Replay
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            summary: PCAP replay status
            tags:
                - Replay
        post:
            operationId: postApiV1Replay
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 146800640 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
            summary: Start a PCAP replay
            tags:
                - Replay
    /api/v1/runtime:
        get:
            operationId: getApiV1Runtime
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
    /api/v1/scenario/generate:
        post:
            operationId: postApiV1ScenarioGenerate
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
    /api/v1/scenario/packs:
        get:
            operationId: getApiV1ScenarioPacks
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
    /api/v1/scenario/profiles:
        get:
            operationId: getApiV1ScenarioProfiles
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
    /api/v1/scenario/profiles/captured:
        post:
            operationId: postApiV1ScenarioProfilesCaptured
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
    /api/v1/segments:
        get:
            operationId: getApiV1Segments
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
    /api/v1/sessions:
        get:
            operationId: getApiV1Sessions
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            summary: List simulation sessions
            tags:
                - Sessions
    /api/v1/sessions/{id}:
        delete:
            operationId: deleteApiV1SessionsId
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
            summary: Stop and remove one session
            tags:
                - Sessions
        get:
            operationId: getApiV1SessionsId
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            summary: Read one session
            tags:
                - Sessions
        parameters:
            - description: Path parameter.
              in: path
              name: id
              required: true
              schema:
                type: string
        post:
            operationId: postApiV1SessionsId
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
            summary: Act on one session
            tags:
                - Sessions
        put:
            operationId: putApiV1SessionsId
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
            summary: Update one session
            tags:
                - Sessions
    /api/v1/simulation:
        delete:
            operationId: deleteApiV1Simulation
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 62915584 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
            summary: Stop the simulation
            tags:
                - Simulation
        get:
            operationId: getApiV1Simulation
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            summary: Simulation status
            tags:
                - Simulation
        post:
            operationId: postApiV1Simulation
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 62915584 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
            summary: Start a simulation
            tags:
                - Simulation
        put:
            operationId: putApiV1Simulation
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 62915584 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
            summary: Select the active simulation
            tags:
                - Simulation
    /api/v1/simulation/preflight:
        post:
            description: |
                Runs the fabric compile and returns every diagnostic. A start that would fail returns the same codes here first.
            operationId: postApiV1SimulationPreflight
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 62915584 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
            summary: Validate a simulation request without starting it
            tags:
                - Simulation
    /api/v1/stats:
        get:
            operationId: getApiV1Stats
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            summary: Simulation counters
            tags:
                - System
    /api/v1/stream/logs:
        get:
            operationId: getApiV1StreamLogs
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            summary: Server-sent log stream
            tags:
                - Streams
    /api/v1/stream/packets:
        get:
            operationId: getApiV1StreamPackets
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            summary: Server-sent packet stream
            tags:
                - Streams
    /api/v1/stream/status:
        get:
            operationId: getApiV1StreamStatus
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            summary: Server-sent status stream
            tags:
                - Streams
    /api/v1/synthesize-walk/models:
        get:
            operationId: getApiV1SynthesizeWalkModels
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
    /api/v1/templates:
        get:
            operationId: getApiV1Templates
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
    /api/v1/templates/{name}:
        get:
            operationId: getApiV1TemplatesName
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            tags:
                - Templates
        parameters:
            - description: Path parameter.
              in: path
              name: name
              required: true
              schema:
                type: string
    /api/v1/templates/use:
        post:
            operationId: postApiV1TemplatesUse
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
    /api/v1/topology:
        get:
            operationId: getApiV1Topology
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            summary: Topology graph for the selected session
            tags:
                - Topology
    /api/v1/topology/export:
        get:
            operationId: getApiV1TopologyExport
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            summary: Export the topology
            tags:
                - Topology
    /api/v1/version:
        get:
            description: Version of the API surface. Build metadata lives at /__version.
            operationId: getApiV1Version
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            summary: API version
            tags:
                - System
    /api/v1/walk/analyze:
        post:
            operationId: postApiV1WalkAnalyze
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
    /api/v1/walk/capture-profile:
        post:
            operationId: postApiV1WalkCaptureProfile
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
    /api/v1/walk/fix:
        post:
            operationId: postApiV1WalkFix
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
    /api/v1/walk/import:
        post:
            operationId: postApiV1WalkImport
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 100664320 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
    /api/v1/walk/list:
        get:
            operationId: getApiV1WalkList
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
    /api/v1/walk/validate:
        post:
            operationId: postApiV1WalkValidate
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
    /api/v1/walk/validate-all:
        post:
            operationId: postApiV1WalkValidateAll
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid CSRF token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Request body larger than 1048576 bytes.
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Rate limit exceeded for this route class.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            security:
                - BearerAuth: []
                  CsrfToken: []
    /metrics:
        get:
            operationId: getMetrics
            responses:
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Missing or invalid bearer token.
                "405":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Method not allowed; the Allow header lists the accepted set.
                "500":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Internal error.
            summary: Prometheus metrics
            tags:
                - System
security:
    - BearerAuth: []
servers:
    - description: Installed loopback daemon (paths are absolute from the origin)
      url: https://localhost:8445
    - description: Example remote deployment
      url: https://niac.example.com
