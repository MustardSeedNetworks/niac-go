.nh
.TH "NIAC" "1" "Sep 2026" "NIAC" "NIAC Manual"

.SH NAME
niac-config-export - Export configuration to YAML


.SH SYNOPSIS
\fBniac config export   [flags]\fP


.SH DESCRIPTION
Export a NIAC configuration file to normalized YAML format.

.PP
This command:
- Loads and validates the input configuration
- Normalizes all fields and structures
- Exports to clean YAML format
- Useful for converting legacy .cfg to YAML


.SH OPTIONS
\fB-h\fP, \fB--help\fP[=false]
	help for export


.SH OPTIONS INHERITED FROM PARENT COMMANDS
\fB--storage-keep\fP=500
	Run history records to keep, pruned oldest first on start (0 keeps every run)

.PP
\fB--storage-path\fP=""
	Path to NIAC run history database (default: ~/.niac/niac.db)


.SH EXAMPLE
.EX
  # Export to new file
  niac config export config.yaml normalized.yaml

  # Convert legacy .cfg to YAML
  niac config export legacy.cfg new-config.yaml

  # Validate and normalize
  niac config export messy.yaml clean.yaml
.EE


.SH SEE ALSO
\fBniac-config(1)\fP


.SH HISTORY
4-Sep-2026 Auto generated by spf13/cobra
