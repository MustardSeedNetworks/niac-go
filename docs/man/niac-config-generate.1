.nh
.TH "NIAC" "1" "Sep 2026" "NIAC" "NIAC Manual"

.SH NAME
niac-config-generate - Interactive configuration generator


.SH SYNOPSIS
\fBniac config generate [output-file] [flags]\fP


.SH DESCRIPTION
Interactive configuration generator for NIAC.

.PP
Prompts you for all configuration details and generates a complete YAML
configuration file. More detailed than 'niac init' template wizard.

.PP
The generator will ask you for:
  - Network name and subnet
  - Number of devices
  - Device details (type, name, IP, MAC)
  - Protocols to enable (LLDP, CDP, SNMP, DHCP, DNS, etc.)
  - Protocol-specific configuration


.SH OPTIONS
\fB-h\fP, \fB--help\fP[=false]
	help for generate


.SH OPTIONS INHERITED FROM PARENT COMMANDS
\fB--storage-keep\fP=500
	Run history records to keep, pruned oldest first on start (0 keeps every run)

.PP
\fB--storage-path\fP=""
	Path to NIAC run history database (default: ~/.niac/niac.db)


.SH EXAMPLE
.EX
  # Generate configuration interactively
  niac config generate

  # Generate with specific output file
  niac config generate my-network.yaml

  # Validate and run
  niac config generate network.yaml && niac validate network.yaml
.EE


.SH SEE ALSO
\fBniac-config(1)\fP


.SH HISTORY
4-Sep-2026 Auto generated by spf13/cobra
