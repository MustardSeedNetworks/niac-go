apiVersion: v1
kind: Secret
metadata:
  name: niac-secrets
  labels:
    app: niac
type: Opaque
stringData:
  # SECURITY WARNING: Change this token before deploying to production!
  # Generate a secure token with: openssl rand -base64 32
  api-token: "CHANGE_ME_BEFORE_DEPLOY"
