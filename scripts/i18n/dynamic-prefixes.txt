# Per-repo allowlist for check-keys.py. One prefix per line; comments
# with #. Matches either a flat key (`section.field`) or namespace-
# qualified form (`namespace:section.field`).
#
# Use when a JSON key is consumed via a dynamic lookup that the static
# analyzer can't see — typically t(`prefix.${variable}.suffix`) or a
# data-driven render path that reads from <ns>.json[<section>].
#
# Keys matching one of these prefixes won't be reported as "unused".
# Keep this list narrow and document WHY each entry is here so a
# future cleanup pass can confirm whether the dynamic path still
# exists.

# --- niac-go ---

# devices.types.* — t(`devices:types.${device.type}`) used wherever a
# device is rendered (DeviceList, DeviceCard, RunningDevices,
# Topology nodes, packet headers). Type IDs come from the daemon.
devices:types.

# devices.list.* — DeviceListContext maps t(`devices:list.${field}`)
# for table headers; the field names are columns in the data table.
devices:list.

# devices.editor.* — DeviceEditor reads field labels via
# t(`devices:editor.${formField}`) from the schema definition.
devices:editor.

# devices.running.* — RunningDevicesPage uses t() over runtime fields
# (noDevicesRunning, startHint) that route through a single helper.
devices:running.

# protocols.common.* — ProtocolTree shared fields rendered via
# t(`protocols:common.${field}`).
protocols:common.

# protocols.categories.* — ProtocolList groups by category, reading
# t(`protocols:categories.${id}`).
protocols:categories.

# errors.generic.* / errors.validation.* — error mapper takes an
# error code and looks up t(`errors:generic.${code}`) or
# t(`errors:validation.${code}`).
errors:generic.
errors:validation.

# common.format.* — formatBytes/formatDurationMs/formatUptime/
# formatRelativeTime use t() with computed keys (bytesB/bytesKB/
# durationMs/durationS etc.) keyed off the magnitude.
common:format.

# common.accessibility.* — aria-label values referenced by component
# props rather than literal t() strings; static analysis can't see
# them. Keep all 12.
common:accessibility.

# common.plurals.* — i18next plural keys; consumer side calls
# t('common:plurals.deviceCount', {count}) which the analyzer DOES
# see as the bare 'deviceCount' form. The _one/_other suffixes are
# pluralization variants the analyzer flags as "extras". Cover both.
common:plurals.

# common.app.* — branding strings used in <title>, footer, manifest.
# Referenced via DOM-side property updates, not literal t() calls.
common:app.

# common.emptyState.* — empty-state copy used by table/list shells
# via t(`common:emptyState.${kind}`).
common:emptyState.

# common.errorBoundary.* — rendered by the top-level ErrorBoundary
# in a render-prop pattern that doesn't show as a literal t() call.
common:errorBoundary.

# common.table.* — table shell (selectAll/selected/showingResults)
# rendered by a generic DataTable component that takes labels as
# props from the t() helper.
common:table.

# common.footer.* — footer chrome reads version/copyright from t()
# but the call site is an HOC the static analyzer can't trace.
common:footer.

# errors.api.* / errors.license.* / errors.packet.* /
# errors.simulation.* / errors.walk.* — error mapper indexes
# t(`errors:<category>.${code}`) based on the daemon's error
# classification. The category-prefix structure is intentional.
errors:api.
errors:license.
errors:packet.
errors:simulation.
errors:walk.

# help.drawer.* / help.footer.* — HelpDrawer reads aria-labels and
# search/footer chrome via t(`help:drawer.${field}`), composed by
# the drawer's render-prop callbacks.
help:drawer.
help:footer.

# pages.* — the page registry and a handful of data-driven label tables are
# the only dynamic consumers in this namespace, so they are listed key by key
# rather than as a `pages:<page>.` prefix per page.
#
# A bare per-page prefix preserves the WHOLE namespace, which is far more than
# the dynamic lookups need: configDiff carries 89 keys, debug 57 and
# libraryFiles 51, nearly all of them ordinary copy reached by literal t()
# calls the analyzer reads perfectly well. Preserving them hid dead copy from
# both removeUnusedKeys and the unused-key report — five dead
# dashboard.status.* keys survived every gate that way (#1343).

# usePages() (pageRegistry.ts) builds the route table with
# t(`${i18nKey}.label`) / .title / .description, and reads an OPTIONAL
# eyebrow via t(`${i18nKey}.eyebrow`, { defaultValue: '' }).
pages:dashboard.label
pages:dashboard.title
pages:dashboard.description
pages:dashboard.eyebrow
pages:runtime.label
pages:runtime.title
pages:runtime.description
pages:runtime.eyebrow
pages:devices.label
pages:devices.title
pages:devices.description
pages:devices.eyebrow
pages:deviceLibrary.label
pages:deviceLibrary.title
pages:deviceLibrary.description
pages:deviceLibrary.eyebrow
pages:deviceEditor.label
pages:deviceEditor.title
pages:deviceEditor.description
pages:deviceEditor.eyebrow
pages:topology.label
pages:topology.title
pages:topology.description
pages:topology.eyebrow
pages:automation.label
pages:automation.title
pages:automation.description
pages:automation.eyebrow
pages:traffic.label
pages:traffic.title
pages:traffic.description
pages:traffic.eyebrow
pages:debug.label
pages:debug.title
pages:debug.description
pages:debug.eyebrow
pages:packets.label
pages:packets.title
pages:packets.description
pages:packets.eyebrow
pages:configDiff.label
pages:configDiff.title
pages:configDiff.description
pages:configDiff.eyebrow
pages:walkValidator.label
pages:walkValidator.title
pages:walkValidator.description
pages:walkValidator.eyebrow
pages:libraryWalks.label
pages:libraryWalks.title
pages:libraryWalks.description
pages:libraryWalks.eyebrow
pages:libraryPcaps.label
pages:libraryPcaps.title
pages:libraryPcaps.description
pages:libraryPcaps.eyebrow
pages:newSimWizard.label
pages:newSimWizard.title
pages:newSimWizard.description
pages:newSimWizard.eyebrow
pages:walkAnalyzer.label
pages:walkAnalyzer.title
pages:walkAnalyzer.description
pages:walkAnalyzer.eyebrow
pages:license.label
pages:license.title
pages:license.description
pages:license.eyebrow

# App.tsx's inline PageConfig covers the device editor's new/edit variants
# with the same template-literal shape.
pages:deviceEditor.newLabel
pages:deviceEditor.newTitle
pages:deviceEditor.newDescription
pages:deviceEditor.editLabel
pages:deviceEditor.editTitle
pages:deviceEditor.editDescription

# The label tables below hold their i18n key in a `labelKey` field and render
# it with t(`...${labelKey}`), so the analyzer sees the render site but never
# the key. Listed at the narrowest prefix covering each table.

# TopologyLegend.tsx — DEVICE_TYPES[] and UTILIZATION_BANDS[] labelKeys.
pages:topology.legend.deviceType
pages:topology.legend.utilization

# BpfFilterBar.tsx — PRESETS[] labelKeys, rendered via tPages(preset.labelKey).
pages:packets.bpfFilter.preset

# WalkProfileSource.tsx — t(`walkAnalyzer.profile.${mode}Tab`) over
# ['import','capture'].
pages:walkAnalyzer.profile.captureTab
pages:walkAnalyzer.profile.importTab

# NewSimulationWizardPage.tsx — t(`newSimWizard.steps.${id}`) over the
# wizard's step ids.
pages:newSimWizard.steps.

# BehaviorPhaseActions.tsx — t(`newSimWizard.behaviors.faults.${type}`) over
# the fault types the daemon supports.
pages:newSimWizard.behaviors.faults.

# TierBands.tsx — a constant map from tier name to key, then t(key).
pages:topology.tiers.

# DevicesPage.tsx — a validator returns 'deviceFragmentInvalid' /
# 'deviceFragmentNameMissing' as a key name which is then translated.
pages:devices.deviceFragment

# DevicesStep.tsx — the draft-editing view tabs render
# t(`newSimWizard.devices.${tab}Label`) over ['visual','behaviors','yaml'].
pages:newSimWizard.devices.visualLabel
pages:newSimWizard.devices.behaviorsLabel
pages:newSimWizard.devices.yamlLabel

# FleetGeneratorCard.tsx — two data-driven label loops:
# t(`newSimWizard.fleet.${field}`) over ['domain','snmpCommunity',
# 'attachmentName'], and t(`newSimWizard.fleet.counts.${field}`) over the
# whole device-count table.
pages:newSimWizard.fleet.domain
pages:newSimWizard.fleet.snmpCommunity
pages:newSimWizard.fleet.attachmentName
pages:newSimWizard.fleet.counts.

# ScenarioPackPicker.tsx — t(`newSimWizard.fleet.packMetadata.${pack.id}.name`)
# and .description; pack ids come from the daemon's pack catalogue.
pages:newSimWizard.fleet.packMetadata.

# protocols.filters.* / protocols.labels.* — discovery-protocol UI
# (CDP/LLDP/EDP/FDP filtering chip + neighbor table) reads filter
# names and labels dynamically.
protocols:filters.
protocols:labels.

# settings.network.disconnected — used as a status fallback when
# the daemon's WebSocket reports disconnect, computed via state
# transition rather than a literal t() call.
settings:network.disconnected
