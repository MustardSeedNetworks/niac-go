# Schema fields the device editor cannot set yet (ratchet; see check-authoring-parity.py).
# Bind a field in schema-bindings.json, or allow-list it with a reason, then remove it here.
attachments[].connect
attachments[].name
behavior_timelines[].name
behavior_timelines[].phases[].duration_ms
behavior_timelines[].phases[].faults[].device
behavior_timelines[].phases[].faults[].interface
behavior_timelines[].phases[].faults[].type
behavior_timelines[].phases[].faults[].value
behavior_timelines[].phases[].name
behavior_timelines[].phases[].reset
behavior_timelines[].phases[].start_offset_ms
behavior_timelines[].phases[].traffic[].device
behavior_timelines[].phases[].traffic[].interface
behavior_timelines[].phases[].traffic[].utilization
behavior_timelines[].repeat_count
behavior_timelines[].start_offset_ms
capture_playbacks[].file_name
capture_playbacks[].loop_time
capture_playbacks[].scale_time
discovery_protocols.cdp.enabled
discovery_protocols.cdp.interval
discovery_protocols.edp.enabled
discovery_protocols.edp.interval
discovery_protocols.fdp.enabled
discovery_protocols.fdp.interval
discovery_protocols.lldp.enabled
discovery_protocols.lldp.interval
networks[].name
networks[].subnet
networks[].virtual_vlan
